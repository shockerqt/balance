{
  "name": "balance",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npm run dev:server",
    "dev:mobile": "REACT_NATIVE_DEVTOOLS_ENABLED=0 npm --prefix apps/mobile run start",
    "dev:server": "cargo watch -x run -p server",
    "mobile": "REACT_NATIVE_DEVTOOLS_ENABLED=0 npm --prefix apps/mobile run start",
    "build": "cargo build -p server",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0"
  },
  "private": true
}
